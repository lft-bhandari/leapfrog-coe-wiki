[project]
name = "api"
version = "0.1.0"
description = "CoE Wiki API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx2>=2.13.0",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "uvicorn[standard]>=0.53.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
